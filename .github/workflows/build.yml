name: Android Build and Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x sa_event_hub/gradlew

      - name: Run unit tests
        working-directory: sa_event_hub
        run: ./gradlew test

      - name: Build debug APK
        working-directory: sa_event_hub
        run: ./gradlew assembleDebug

      - name: Upload the APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: sa_event_hub/app/build/outputs/apk/debug/app-debug.apk